<% content_for :title do %>
  <%= @company.name %> | RelyApply
<% end %>

<% if mobile_device == ("mobile" or "tablet") %>
  <%= render 'show_company_mobile'%>
<% else %>
  <%= render 'show_company_desktop'%>
<% end %>



























     