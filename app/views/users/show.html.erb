<% content_for :title do %>
  My Profile | Rely Apply
<% end %>

<% if mobile_device == ("mobile" or "tablet") %>
  <%= render 'show_profile_mobile'%>
<% else %>
  <%= render 'show_profile_desktop'%>
<% end %>